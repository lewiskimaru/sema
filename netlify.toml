[build]
  base = "sema-frontend"
  publish = "dist"
  command = "npm run build"
  ignore_warnings = true

# Handle SPA routing with History API
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 