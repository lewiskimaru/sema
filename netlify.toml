[build]
  base = "sema-frontend"
  publish = "dist"
  command = "npm install --legacy-peer-deps && npm run build"

# Handle SPA routing with History API
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200 